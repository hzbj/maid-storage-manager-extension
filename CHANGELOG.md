# 更新日志

## 1.2.7

- 修复末影腰包远程打开快递员设置时仍依赖客户端实体追踪的问题：超过追踪距离或主人与女仆不在同一维度时，站点列表不再消失，运输确认、默认站点、审批及仓管扩展设置会通过上游已验证的远程会话送达真实女仆。
- 快递员设置按钮改用菜单携带的稳定实体 ID，不再因客户端远程代理短暂不可见而静默丢弃操作；末影腰包传输段不再受主人距离或维度限制，扫帚航段的主世界限制保持不变。

## 1.2.6

- 扩展全局设置统一归入“女仆仓管 → 女仆仓管扩展”子分类；巡检周期、快递员授权和信箱审批移动到仓管任务设置末尾的独立入口，无待审批申请时会明确显示空状态。
- 快递员与仓管、主人之间改为 2.75 格内附近交接：不再强制双方互相面对、不再停止仓管导航，也不再等待 1 秒；交付箱逻辑保持不变。
- 末影腰包运输完成后，快递员会在仓管交接点开启 Home 待命并清除临时跟随覆盖，不再尝试返回主人身边。
- 兼容基线升级为车万女仆 1.5.3 与《车万女仆：万法皆通》1.8.2；使用后者新增的远程女仆快照与会话机制，使装备末影腰包的女仆可在超出实体追踪范围时正常打开界面。

## 1.2.4

- 重绘杂物箱标记、物流查看表和库存巡检器的 16×16 物品图标：杂物箱改为装有多种物品的开口木箱，物流查看表改为带货箱路线的羊皮纸夹板，巡检器改为检查木箱的斜向放大镜。
- 收紧图标轮廓和像素密度，保留透明呼吸区，确保三种图标在物品栏原始尺寸下仍能直接辨认用途。

## 1.2.3

- 快递员任务不再要求预先同时装备末影腰包和扫帚；任务可直接启用，每次运输开始时再按距离选择的路线检查道具。
- 四种运输方式改为独立限制：近主人且近仓管时步行无需道具，远仓管只需扫帚，远主人只需末影腰包，两边都远才需要两者。
- 只有实际使用扫帚的路线才要求有效快递仓库信箱；单纯携带扫帚不再阻止步行或末影腰包路线。

## 1.2.2

- 降低快递仓库信箱的合成成本：移除两颗末影珍珠，并允许使用任意木质栅栏。

## 1.2.1

- 请求列表仅在送达坐标或维度实际变化时提示“已记录本次快递送达位置”；在同一位置反复打开界面不再刷屏，固定送达点仍会正常保存。
- 修复杂物已进入目标箱、女仆背包已空时任务表仍保留幽灵在途数量，导致女仆持续卡在开箱阶段的问题；物理搬运完成后先提交精确 NBT 账本，再刷新共享库存缓存。
- 普通箱投放期间新增保守完成校验：仅在同一开箱操作内同时确认女仆对应货物减少、当前目标箱对应货物等量增加时修正陈旧账本；玩家单独拿走货物仍保留防丢失恢复，不会误判完成。

## 1.2.0

- 修复交付箱被破坏后快递员仍持续向旧坐标寻路：目标区块已加载且物品容器能力消失时会立即注销交付箱，有货则降级为交给主人，无货则原地待机；未加载区块不会被误判为箱子已销毁。
- 物流查看表支持潜行右键非容器方块注销交付箱；界面新增“清除工作”按钮，地面女仆立即清除当前快递流程并在原地 Home 待机，空中女仆收起扫帚、瞬移到附近最近的安全地面再清除，物资与待归还需求单保留。
- 快递员启用条件统一收紧为同时装备末影腰包并携带魔理沙的扫帚；任务选择、运输模式分流、中途道具校验和中英文提示均要求两者缺一不可，魔法皆通附属改为必需依赖。
- 仓管扩展设置接入车万女仆右侧“全局设置”的 Cloth Config 页面，并按当前打开的仓管女仆加载独立设置；移除原仓管配置页末尾通过反射注入的“打开”入口。
- 修复“等待主人领取”状态在主人仍处于 8 格范围内时自动重启寻路、连续失败后反复刷屏的问题；现在只有主人真正进入 2.75 格交接距离或主动更新定位时才会恢复交接，主人直接走到女仆身边也可领取。
- 修复物流查看表标记交付箱后仍把物资交给主人，以及入箱后恢复跟随的问题：交付箱会优先接管所有未完成的主人交付阶段，物资入箱后以箱旁当前位置开启 Home 模式等待，新需求单或存货确认仍可唤醒下一轮运输。
- 修复快递运输期间误用车万女仆 Home 模式的问题：Home 日程中心不再每 40 tick 抢占起落点/主人寻路，运输期单独屏蔽跟随，完成后再按设置恢复。
- 请求列表右键会记录主人当时的位置作为固定送达目标；物流查看表新增“更新定位”按钮，可把当前主人位置同步为本次任务的新目标。
- 修复杂物批次投放每执行一轮就无条件关闭普通箱上下文、账本仍有同目标在途货物时下一 tick 又原地重开同一箱的问题；同一物理目标箱现在会复用一个打开上下文，直到对应货物全部送达或明确推进到其他候选箱，回源余量也不会再逐 tick 重开源箱。
- 原注册名 `courier_warehouse_station` 保持兼容，显示与功能改为“快递仓库信箱”：先右键标记露天、平整的 3×3 起落区，再在同维度 64 格内放置信箱；信箱不得占用起落区，潜行右键可重新标记或重新发起绑定。
- 信箱会在自身 64 格内优先绑定放置者拥有的最近仓管：仓管主人亲自放置时直接成立，不需要快递员或第二次审批；代放给其他玩家的仓管时，才进入仓管主人设置页等待同意或拒绝。获批站点由该仓管已授权的全部快递员共享。
- 快递绑定数据拆分为信箱坐标与实际起落坐标；飞行、返航和召回使用起落区，站点有效性使用信箱方块实体。信箱被破坏、审批失效或 3×3 净空被遮挡时运输拒绝启动，并在物流查看表显示站点无效。
- 运输按快递员与主人、仓管的水平距离自动分为四种：双方都近时原生步行；近主人、远仓管时扫帚飞往信箱；远主人、近仓管时步行交接后由末影腰包远程传输；双方都远时扫帚飞往信箱后由末影腰包传输。凡快递员持有扫帚都必须先有有效信箱站点，每次飞行前都会再次检查起飞环境。
- 快递员现在最多绑定 6 个获授权仓管，并可在女仆设置页选择默认站点；物流查看表改为显示运输站点，不再是多仓管绑定的必要条件。
- 物流查看表新增扫帚飞行紧急召回：卡住时可把快递员安全送回默认站点，运输清单、货物和防丢失暂存不清空。
- 修复仓管取货回程隔墙卡住：上游回程超时后先调用车万女仆安全传送完成面对面交接，再保留原虚拟物品兜底。
- 修复仓管结束遗留或格式异常的需求列表任务时，上游 1.15.6 无条件读取缺失 `uuid` 导致实体 Tick 崩溃、随后触发 Watchdog 二次重启的问题；结束任务前仅为缺失或类型错误的任务 UUID 补齐安全标识，已有 UUID 保持不变，需求列表仍按原流程归还并清理任务状态。
- 完善扫帚快递的安全航线：起飞阶段先原地垂直爬升至巡航高度再水平飞行；起降点要求平整空旷，落点按区块逐圈扩展并在连续失败后改选，搜索到设置上限仍无安全点时返航并同步物流状态。
- 快递员地面交接改用车万女仆原生寻路与安全传送兜底，运输事务期间维持所在区块加载；无法继续接近主人时会携货原地等待，不再因主人距离过远而停在仓管处。
- 物流查看表支持潜行右键容器设置交付箱；快递员可把取回物资送入交付箱，箱满、不可达、远离主人及等待领取等情况都会保留货物并显示明确状态。
- 杂物批次在途时会在官方仓管日程执行前锁定 `NO_SCHEDULE`，避免普通 `PLACE` 周期性清除当前寻路目标；混合批次中无目标货物改为等待当前投放完成，不再与“正在送往目标箱”同时宣称正在返程。
- 进度表在杂物清理阶段不再只修改顶部标题：正文会按当前批次显示真实物品卡、携带数量/批次数量，以及“从杂物箱取货、送往目标箱、退回原杂物箱”状态；批次尚未装载时显示下一批待取物品，不再错误显示“目前没有任务”。
- 修复杂物整理和库存清单刷新每 20 tick 无条件重新下发 `moveTo`、持续重建车万女仆路径造成移动一卡一卡的问题；活动路径只下发一次，仅当导航已经结束但仍未到达时才按有界间隔重新选点寻路。
- 新增进度正文映射与导航重派策略回归测试，覆盖“女仆已携带货物但任务列表为空”和“进行中的路径不得按秒重建”。
- 重制快递仓库信箱、物流查看表、任务铃、杂物箱标记及物流界面像素资源；物流界面采用暖棕、羊皮纸、金色边框与少量紫青点缀，不烘焙文字、不放宝石和文字底色长条，任务铃物品图标仅保留铃铛本体，已放置任务铃模型不变。
- 扫帚航段改为固定站点接驳：仓管交接后先用车万女仆原生寻路返回 3×3 起落点再起飞；抵达仓库时先落在站点、收起扫帚，再步行到仓管面前。扫帚移动使用连续实体移动，不再逐 tick 同步坐标传送，扫帚路线也禁用地面“末影人式”寻路传送。
- 快递员任务作为独立物流工作实体持续加载快递员、当前仓管、信箱和起落点区块；主人远离后物流查看表仍可取得实时快照，不再等主人走近才恢复事务。
- 主人附近的动态落点继续按 X/Z 距离在可配置的 32～64 格范围逐圈搜索；无法找到空旷平整落点时停止运输并更新物流状态。落地后恢复车万女仆原生跟随，也可在快递员设置中改为抵达后原地 Home。
- 扫帚运输严格限制在主世界：快递员、仓管站点或主人不在主世界，以及任何需要跨维度的扫帚航段，均不会启动；返程途中主人离开主世界时扫帚悬停，主人返回后继续。
- 紧急召回改为检查真实扫帚载具：只有女仆已经起飞、仍在空中时按钮可用；尚在步行前往起落点或已经落地时无法召回。
- 修复远程扫帚运输在玩家离开模拟距离后停止 Tick：原区块票据距离 `1` 只能维持区块 Tick，现提升到 Minecraft 实体 Tick 所需的最低距离 `2`，并持续覆盖快递员、扫帚、仓管、信箱和起落点；物流距离不再冻结，仓管目标名也不会因实体卸载退化为无名类型。
- 修复仓管交接后无法走回固定起落点：除仓管目标记忆外，同时向女仆当前原生导航器派发路线，路径结束后才按秒重试；已验证的 3×3 起落区内任意站立格均可触发起飞，不再强迫女仆精确踩中中心点。
- 近距离步行返主不再调用车万女仆安全传送兜底，避免在仓管与主人附近反复闪现；仓管方向仍保留该兜底，返主改为原生路径，确实无法接近时按既有规则携货等待。
- 修复仓管接到快递需求、入库或退货任务后在玩家离开模拟距离时停滞：所有运输方式的在途日志都会持续续票，并以实体 Tick 等级覆盖仓管当前位置、完整工作范围、当前寻路目标和交互箱；仓管跨入相邻区块后仍可继续取货、存放和返回快递员，临时切换任务也不会提前释放加载生命周期。
- 修复仓管交接后快递员已走到起落区边缘却永久不起飞：原生寻路停在标记 3×3 外一格时，会实时复验女仆脚下新的 3×3 是否平整、露天且净空，安全则直接从当前位置生成扫帚垂直起飞，不瞬移；连续三次寻路停滞或扫帚生成失败会提示主人并继续改选格位重试。

## 1.1.5

- 修复 1.1.4 批量杂物账本返回不可修改列表，整理初始化或从杂物箱提取物品时触发 `UnsupportedOperationException` 并导致服务端崩溃重启的问题；行为层现在取得可修改的深拷贝，提交后再原子替换持久账本。
- 杂物箱收集、目标箱投放及回源移动在保留女仆仓管目标记忆的同时，直接调用车万女仆当前 `PathNavigation`；到达、失败或行为停止时会清除并停止本次路线。
- 库存清单展示框刷新同样使用车万女仆当前导航，并每秒重新选择可达站位、续派路线，避免只写目标记忆却原地等待，最终误报路径超时。
- 新增批次货物快照可修改且与持久状态隔离的回归测试，覆盖服务器实际崩溃路径。

## 1.1.4

- 杂物整理改为“一个源杂物箱一个批次”：从完整巡检缓存一次筛选多种可归类物品，按物品轮询占用女仆空槽，再按目标箱聚合投放；容量不足时自动分批。
- 保持“白名单过滤箱最高优先级 → 已有同类物品箱 → 无目标则留在杂物箱”，普通空箱不再作为任何整理兜底。
- 每只仓管女仆新增“整理时匹配 NBT”开关，旧存档默认关闭；关闭时普通同类目标只比较物品 ID，开启时比较完整 NBT。白名单箱仍以过滤器自身的 NBT 规则为准。
- 提取、运输、部分插入和回源全程使用精确 NBT 批次账本；切换任务、保存重载或玩家挪动运输槽不会把在途物品误当成已完成。
- 每轮巡检记录独立代次、同物品忽略键和“物理目标＋精确 NBT”满箱证据；官方 ItemHandler 按实际侧面做无写入模拟插入，网络仓储只在真实插入返回余量后标记已满。
- 杂物批次与快递事务新增互斥：已在途的杂物先安全送达或回源，活跃快递事务期间不会再启动新杂物批次，双方不会相互窃取货物或覆盖事务状态。
- 原仓管“进度表”在杂物清理阶段显示已完成/总源杂物箱数；同一箱分批不重复计数，余量回源完成前不提前进度，保存重载后仍能继续显示。
- 切换“整理时匹配 NBT”会排队一次性完整巡检；即使周期巡检设为关闭，本次重规划仍会执行，完成后保持周期巡检关闭。
- 物流查看表标题统一为“物流查看表”，女仆及目标的可翻译模型名称改由客户端解析，修复显示 `model.touhou_little_maid...name` 原始键的问题。
- 重新排布标题、女仆名、运输方式、状态、目标、距离、货物和连接状态，手持及小展示框均保留独立行距，避免文字与槽位重叠。
- 移除物流查看表底图中的全部宝石、文字后方长条色块和面板装饰线，只保留暗紫框架、平面黄铜角件、羊皮纸与六个货物槽。
- 扫帚航段不再从交接位置直接起飞或向实体坐标直降：女仆先步行到可达、露天且具有垂直净空的安全起飞点，落地时选择目标 4～12 格外的安全点，再收起扫帚步行到主人或仓管面前。
- 飞行移动增加扫帚与女仆双重碰撞检测，并按前方地形动态抬升巡航高度；遇到障碍只上升或悬停，不再用坐标推进穿过墙体。

## 1.1.3

- 快递流程拆分为两种真实运输方式：末影腰包快递员驻守仓管工作区，借原远程面板与主人交换物品；扫帚快递员必须从主人 8 格内出发，乘魔理沙扫帚高空往返仓管与主人。
- 扫帚事务期间临时关闭普通跟随并持久化原回家模式，返回主人身边后恢复；末影腰包驻仓传输不修改玩家原先的跟随设置。
- 仓管与快递员仍需进入 2.75 格、停下并面对 1 秒完成清单和物资交接；满载、背包扩容栏位及完整 NBT 防丢失暂存继续生效。
- 物流查看表取消相对地图和三方坐标标记，改为实时显示运输方式、当前状态、正在前往的目标、距离与货物。
- 实时物流数据从物品 NBT 改为独立网络快照，修复手持时反复收起/拿起；展示框不再每秒替换框内物品，并为 64×64 小展示框提供独立紧凑排版。
- 修复快递员启用条件拼接出原始翻译键；现在用中文明确显示“末影腰包或魔理沙的扫帚，任意一种即可”。
- 物流查看表底图从地图改为暗紫、黄铜、青色指示灯组成的奇幻像素物流仪表页。

## 1.1.2

- 完全重写巡检后的杂物整理：巡检一次只派发当前位置最近的箱子，只有官方查看上下文完成并写入最新缓存才记录成功；队列目标异常消失不再被误判为已查看。
- 删除旧的杂物独立快照、队列消失推断、清理规划器、清理状态机和一次性杂物调试链路；新整理只从整轮成功巡检后的统一最新缓存规划。
- 空白名单箱只要接受精确物品就具有最高优先级；其次才是最新缓存已有同物品 ID 的普通箱。无匹配目标时保持杂物箱原状，普通空箱绝不兜底。
- 一轮遍历全部杂物箱和全部物品种类；目标部分接收后继续尝试匹配目标，最终余量只回原杂物箱，回源暂时失败则保留事务槽重试。
- 在途物品改为跨全部匹配栏位核对；挪槽、拆分或暂时取走时不会清除事务日志。提取后切换女仆任务也会启动独立回源恢复，直到物品退回原杂物箱。
- 新增独立道具“物流查看表”，不替换原有进度表；右键自己的快递员女仆完成绑定，手持或挂入展示框时显示快递员、仓管和主人的相对地图位置。
- 物流查看表每秒从服务端刷新一次授权快照，并展示当前需求、入库、返还及防丢失暂存货物；目标区块卸载后保留最后坐标并明确标记为离线，不会向非主人泄露货物数据。
- 新增物流查看表祭坛配方与中英文界面；潜行右键空气可解除绑定。
- 物流查看表、地图底板和库存巡检器统一重绘为暗紫、黄铜、宝石高光的奇幻像素风；两种表仍保持不同用途和独立外形。

## 1.1.1

- 修复杂物清理把目标识别失败掩盖成“最近普通箱兜底”的问题；清理过程只允许白名单箱或缓存中已有同类物品的箱，其他普通箱严格保持原状。
- 整理索引改为读取工作范围内全部有效箱子缓存，并用物理箱键合并双箱/不同 `Target` 表示；本轮只要求杂物来源完成查看，缓存目标无需在同一轮再次成功查看。
- 快递员任务资格改为“饰品栏装备末影腰包”或“真实可用库存中携带 `touhou_little_maid:broom`（魔理沙的扫帚）”，二者任一即可工作。
- 需求单、取货物资和待存物资不再隔空转移；快递员必须寻路到仓管女仆面前，双方停下、互相面对并完成交接动作后才改变库存。
- 仓管备齐物资后，快递员会寻路追踪主人、当面交货，再返回任务开始点；若从主人 8 格内出发，返程锚点改为主人当前位置。
- 仓管工作期间快递员会在交接点等待，不会停掉仓管的取货/存货寻路；仓库满、快递员满载或主人背包满时均保留完整 NBT 并等待可用空间，不丢地。

## 1.1.0

- 新增车万女仆任务“快递员”；任务只在女仆饰品栏装备 `touhou_little_maid_spell:ender_pocket`（末影腰包）时可用。
- 快递员可在任务设置中申请绑定附近仓管女仆；不同主人的仓管必须由仓管主人在扩展设置中批准，授权同时持久化在仓管侧。
- 主人通过末影腰包原有远程女仆面板投放需求清单后，清单会自动交给已绑定仓管；成功清单回到普通背包栏，失败清单回到副手，并向快递员主人报告缺货。
- 普通物资必须在快递员设置中确认后才会交给仓管分类存储；仓库无法继续接收时，未存物资会原样退回快递员。
- 容量按女仆当前真实可用库存动态计算，包括额外背包扩展栏位和主副手。满载、背包容量缩减或末影腰包被取下时事务只会暂停，不会丢地或销毁物品。
- 巡检逐箱查看改为按女仆实时位置动态选择最近箱；单类回流失败时保留其他物品的待办，不再静默提前结束整轮清理。

## 1.0.7

- 修复巡检后杂物回流遗漏白名单分类箱的问题；金头盔、原木等物品现在会优先进入接受对应精确物品的过滤箱，再回退到已有同类物品的普通箱。
- 保留过滤器自身的 NBT 精确匹配语义，并在实际搬运前重新验证实时过滤规则，避免巡检后过滤器变化导致误投。

## 1.0.6

- 修复巡检完成后，女仆仍在前往库存清单展示框时被固定 10 秒总时限误判为超时的问题。
- 刷新阶段改为只在连续 10 秒没有取得有效移动进展时报告“前往展示框超时”；长距离但持续前进的路线不再失败。
- 移除 Minecraft 行为包装层额外的 12 秒总时限，避免长路线被静默中断并重新开始。

## 1.0.5

本版本汇总并统一发布此前在 1.0.4 开发阶段并行完成的功能：

- 增加杂物箱兜底存放与巡检后回流；按物品 ID 匹配目标，按完整 NBT 精确搬运，并在回流结束后刷新库存清单。
- 增加库存巡检器、周期巡检、展示框库存清单绑定和发布结果反馈。
- 增加任务铃绑定、召回、超时、停留及日程恢复，避免任务铃打断后遗失原工作状态。
- 增加进度表状态显示，覆盖巡检、请求、物流、整理、分类、寻路、战斗、休息等工作阶段。
- 增加可达箱调试命令和仅执行者可见的可达性标记。
- 增加独立扩展设置页，可按女仆配置巡检间隔、扫描半径、任务铃范围及反馈选项。
- 增加旧魔改存档迁移与重复安装保护，兼容旧方块、物品、记忆和配置数据。
- 修复放置流程、存储上下文生命周期及客户端/服务端兼容边界中的稳定性问题。

## 1.0.4

- 1.0.5 功能的并行开发与验证版本。
